from typing import List

def arraysplit(nums: List, m: int) -> int:
    start, end = max(nums), sum(nums)

    while start <= end:
        mid = start + (end - start)//2

        #Split
        #
        #

        if pieces >= m:
            


